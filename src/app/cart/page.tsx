import CartContent from "@/components/cart/CartContent";

export const metadata = {
  title: "Your Bag | Kiks Shop",
  description: "Review your selected items and proceed to checkout.",
};

export default function CartPage() {
  return <CartContent />;
}
